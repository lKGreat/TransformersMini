# ADR-0002 Training Entry and Config

- ID: `ADR-0002`
- Status: `Accepted`
- Related Requirements: `REQ-0001`

## Decision

- Use JSON as the primary configuration format.
- Use a unified `ITrainingOrchestrator` for CLI and WinForms.
- Support `run/train/validate/test` commands with optional overrides.

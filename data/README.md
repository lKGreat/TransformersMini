# Data Layout

Store local datasets under `data/`.

## Suggested structure

- `data/samples/detection/annotations/*.json` (COCO-style)
- `data/samples/ocr/manifest.jsonl`

Real datasets are not committed. Sample configs may point to placeholder paths.

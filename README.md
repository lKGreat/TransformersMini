# TransformersMini

Requirement-driven multimodal training platform skeleton for .NET 10.

- `docs/` drives iterations (PRD + ADR + iteration + task + acceptance)
- Unified train/validate/test entry for detection and OCR
- Dual backend architecture placeholders (`TorchSharp` and `ML.NET`)
- Shared application services used by CLI and WinForms hosts
